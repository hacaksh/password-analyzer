# password-analyzer

A password analysis tool — checks dictionary/leet words, sequences, keyboard patterns, estimates entropy and brute-force time, and prints suggestions.

Run: python -m password_analyzer.analyzer
Or after install: password-analyzer "MyP@ssw0rd123"
